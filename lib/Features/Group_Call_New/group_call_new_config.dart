/// Feature flag for the new native group call module.
/// When enabled, the app uses pure Flutter/mediasoup instead of the WebView-based embedded call.
class GroupCallNewConfig {
  static bool enabled = true;
}
